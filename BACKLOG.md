# BACKLOG.md — Past Simple

Список задач проекта. Подробности см. в правиле 6.3 глобального CLAUDE.md.

**Статусы задач:**
- `[ ]` — todo, ждёт начала
- `[~]` — in progress, в работе
- `[?]` — blocked, ожидает разблокировки

**ID-нумерация в «К релизу»:** стабильные ID `R<N>`. Удалённый ID не переиспользуется (так пропуски в нумерации — норма). Новый пункт получает следующий свободный ID.

--- 

## К релизу

- [ ] **R25** Скилл/скрипт для обработки фото при добавлении нового человека
  Сейчас 43 фото лежат локально в `public/assets/people/<id>.webp` (458px, скачаны с Wikimedia + переконвертированы в WebP). При добавлении нового персонажа через `/add-person` это вручную не делается. Нужен либо отдельный скрипт `scripts/sync-photos.js` (обходит roster, для каждого `id` без локального файла качает с `person.photo`, конвертирует через `cwebp` или `sharp`, кладёт в `assets/people/`), либо встроить в `/add-person`. Используется в about-эффекте hover-фото и в попапах событий (`localPhotoPath` в `js/lib/photo.js`).

- [ ] **R2** Заполнить тексты `event.description` (lead)
  По 200+ событиям описания сейчас 1–2 предложения. Расширить хотя бы у популярных людей. Это про lead, не про `sections` (sections заполнены в сессии 2026-05-04).

- [ ] **R6** Sticky captions при vertical scroll
  Названия мировых событий и метка века должны прилипать к верху Timeflow при vertical scroll. Требует структурного рефакторинга — вынос captions из event-блоков в отдельный `<div class="timeflow__captions-bar">` с `position: sticky; top: 0`. Простой CSS-подход (sticky внутри absolute) не работает чисто.

- [ ] **R9** Empty state — для главного экрана (фильтр пустой) и для сайдбара.

- [ ] **R10** Loading / fetch state — пока подгружаются данные.



---

## Потом

- [?] Hover на event-dot — возраст
  Возможно уже покрыто Current year overlay из сессии 2026-05-04 (вертикальная линия + возрасты в пересечениях с life-line). Нужно подтверждение пользователя перед закрытием/доработкой.

- [?] Аналитика и приватность: Яндекс.Метрика
  Счётчик + cookie banner + страница «Политика конфиденциальности». **Blocked:** нужен Counter ID от пользователя.

- [?] Форма обратной связи в about-попапе
  План: Formspree. **Blocked:** нужна регистрация со стороны пользователя.

- [ ] Раскрывашки в библиотеке на каждый variant
  Сейчас одна раскрывашка на весь компонент. Хочется отдельную под каждым `.ks-state` (модификатором), расположенную ниже самого варианта и выше раскрывашки HTML. Парсер должен собирать токены на уровне модификатора.

- [ ] Дополнительные стили типографики (если потребуются)
  Сейчас есть: h1, h2, lead, body, body-s, semibold-14. По мере необходимости — Display, Caption, Section heading и др.

---

## Технический долг

- [ ] Перенести `_schema.js` из `public/data/` в импортируемое место (например, `js/lib/_schema.js` или эквивалент)
  Сейчас `_schema.js` живёт в `public/`, что в Vite означает статическую раздачу через URL `/data/_schema.js`. Это блокирует чистый ES-модульный импорт констант из `js/`. После переноса: обновить пути в скиллах (`.claude/commands/add-person.md`, `add-event.md`) и в любых местах кода, которые читают этот файл. **Это блокер для двух записей ниже — их можно делать только после этого переноса.**

- [?] Заменить хардкод `START_YEAR`/`END_YEAR` в коде проекта на импорт из `_schema.js`
  **Blocked:** зависит от записи выше (перенос `_schema.js` в импортируемое место).
  Конкретные места из разведки этапа 4 адаптации:
  - `js/main.js:20-21` — `startYear: 1850, endYear: 1950` в `state` объекте (главный runtime-источник правды).
  - `js/main.js:33` — `const DEFAULT_CENTER_YEAR = 1900` (можно вычислять как `(START_YEAR + END_YEAR) / 2`, или оставить независимым — обсудить).
  - `js/popup.js:141` — user-facing copy «промежуток между 1850 и 1950 годами» в about-попапе, переделать через template literal.
  - `js/main.js:449` — комментарий `// Скроллим к центру XX века (1900)` (низкий приоритет).
  - `js/timeflow.js:50, 53-54, 86, 104, 115` — иллюстративные комментарии «1850 → 1900», «1850-1950» (опционально — сам код уже использует `state.startYear/endYear`).

- [?] Заменить дубликаты enum категорий в коде на импорт `PERSON_CATEGORIES` из `_schema.js`
  **Blocked:** зависит от записи выше (перенос `_schema.js` в импортируемое место).
  Конкретные места из разведки этапа 4 адаптации:
  - `js/popup.js:15-19, :24` — `CATEGORY_CLASS` объект + fallback `|| 'artist'`.
  - `js/timeflow.js:11-15` — параллельный `CATEGORY_CLASS` (тот же объект что в `popup.js`).
  - `js/timeflow.js:20` — `CATEGORY_ORDER` массив (порядок — отдельное знание для UX-сортировки; сами значения должны прийти из enum).
  - `js/timeflow.js:186, 260, 281, 345, 519, 537` — fallback `|| 'artist'` в шести местах.
  - `js/sidebar.js:8-13` — конфиг сайдбара (гибрид: значения дублируются, но добавлено `label` и `cssCls` с особым именем `politician → politic` — нужно решить, как разделить общее и проектное).
