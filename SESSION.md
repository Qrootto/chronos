# Past Simple — Session log

Журнал состояния между сессиями. Обновляется в конце каждой сессии (Stop-hook напомнит). При старте новой сессии Claude должен прочитать этот файл первым делом.

---

## Last updated
2026-05-06 (about-попап + большая полировка дизайн-системы и попапа)

## Главное (не теряй между сессиями)

**Ментальная модель — `MAIN_SCREEN.md`.** Это источник истины. Координаты в Figma — иллюстрация одного состояния, **не задание раскладки**.

Главная формула: `x = (year − START_YEAR) × pxPerYear`. Сейчас `START_YEAR=1850, END_YEAR=1950, pxPerYear=12`.

Поведение sticky-имени:
- Линия видна и `born` виден → имя стоит на координате `born`, без стрелки.
- Линия частично в viewport, `born` за краем → имя приклеено к видимому концу линии, **без стрелки** (пользователь видит линию).
- Линия полностью за viewport → имя у ближайшего края со стрелкой (`◀ Имя` или `Имя ▶`) — индикатор «там есть человек».

Связи между людьми (`connections[]`) появляются **только при hover события**, рисуются как 1px gradient-линия от категории-А к категории-Б. Hover на любую из двух связанных точек рисует одну и ту же связь.

Подписи мировых событий скрыты, кроме меток веков (XIX, XX и т.п.) — они видны всегда.

---

## Свежие сессии (2026-05-05 — 2026-05-06)

### Что сделано
- **Попап «О проекте»** (about-popup): открывается по клику на `i` в шапке. Логотип + текст с ссылкой на ermolaev.space. Использует ту же шторку, что и event-popup (`.popup-overlay`), но с модификатором `.popup--about` и block-layout для надёжного скролла.
- **Дизайн-система — большая ревизия**:
  - Переименование `--text-primary` → `--text-tertiary` (приглушённый); новый `--text-primary` = `--light-100` (контрастный).
  - Все яркие primitives (red/green/blue/yellow/purple) и новый `--toxic` (заменил `--orange`) — теперь полные alpha-шкалы 4..100 (как у white/light/black/dark).
  - `--dark-*` переделан под полупрозрачную шкалу на базе `#3b5172`.
  - Новые токены: `--surface-century` / `--text-century` (на `--red-64`), `--text-secondary` (`--light-64`), `--spacing-3xs` (2px), `--spacing-4xl` (72px).
  - Удалено 5 неиспользуемых semantic-токенов.
  - `--brand` → `#8856fd` (фиолетовый); `--purple-*` база → `#b697ff`; `--yellow-*` → `#feac39`; `--toxic-*` → `#c7ef42`.
  - Все pixel-spacing значения переведены на токены (где совпадают). Несовпадающие обсуждены и заменены на ближайшие.
  - Шрифты: lead 300/34px, h2 300/Light, body 300/26px (теперь все через токены, не хардкод).
- **Утилита типографики** (`js/lib/typography.js`): `fixOrphans` / `fixOrphansInTree` — убирает висячие однобуквенные предлоги в конце строки. Применяется в попапах. Зафиксировано как принцип в CLAUDE.md.
- **Connection lines** переделаны: вертикальные gradient-линии `<div>` (вместо SVG-дуг). Default 1px, hover 3px (растёт симметрично из центра). Центрированы по точкам.
- **Кликабельные имена в «В это же время»**: hover сдвигает на 4px вправо, click открывает попап события этого человека в том же году.
- **Zoom + scroll-hint**: тёмный фон, тонкий padding (2/4 px), pill-shape (`--radius-xxxl`); zoom переехал в левый нижний, scroll-hint в правый.
- **Year hover**: hover в Year scale теперь приобретает размеры Active. Current year overlay: вертикальная линия от курсора + возрасты в пересечениях с life-line. **Hover на event-dot** включает visual hover линии + цвет категории. **Hover на саму life-line** подсвечивает только возраст (`--light-32`), без visual hover линии.
- **Чекбоксы в сайдбаре**: активные чекбоксы окрашиваются в цвет своей группы (политики красные, писатели фиолетовые и т.д.) через CSS-переменную `--checkbox-active-color`. События — на `--brand` по дефолту.
- **Раскрывашки токенов/компонентов** в `/library`: блок «Примитивы» теперь сворачиваемый. Под каждым компонентом раскрывашка с зависимостями и токенами (автогенерируется из CSS, см. `js/build/generate-components-meta.js`).
- **Зафиксировано в CLAUDE.md**:
  - **Маркировка вопросов** Q1, Q2, Q3 (с большой Q), уточнения Q1.1, Q1.2 — для всех будущих проектов.
  - **Правило про размеры**: пиксели от пользователя → токен с тем же значением. Если такого нет — спросить.
  - **Правило про неясности**: если правка может сломать (удалить/переименовать токен, изменить layout) — задаю вопросы, не делаю сразу.
  - **Правило про типографику**: висячие однобуквенные предлоги → ` ` через `fixOrphans`.
  - **Правило про библиотеку как источник правды**: каждый токен/компонент должен быть на `/library`.

### Не сделано (отложено)
- **Sticky captions** при vertical scroll (см. BACKLOG → раздел): названия мировых событий и метка XX должны прилипать к верху Timeflow. Требует структурного рефакторинга — выноса captions из event-блоков в отдельный `position: sticky` bar. Откладываем до отдельной задачи.
- **Раскрывашки в библиотеке на каждый variant** (а не один на компонент целиком) — упомянуто в очереди ниже, но пока не сделано.
- **Форма обратной связи** в about-попапе — пользователь упомянул в тексте «через форму ниже», но саму форму ещё не делали (план: Formspree, нужна регистрация на стороне пользователя).

---

## Свежие сессии (2026-05-04)

### Что сделано
- **Заполнение sections для всех событий**: 6 батчей по 8 человек + точечный patch на 14 событий, источник — WebFetch на ru/en.wikipedia. Покрытие **181/181 событий, 43/43 человек**. Тексты — черновики до факт-чек прохода. Скрипт-сидер: `js/build/seed-sections.js` (поддерживает поле `match` для дизамбигуации событий с одним годом).
- **Сэмпл-фактчек**: проверены 15 случайных секций. Найдено 4 неточности (Дебюсси/свадьба, Рокфеллер/95% vs 90%, Моне/годы серии, Форд/девиз) — все исправлены. Уровень брака ~27%, дальше — точечный фактчек по затронутым местам.
- **Подключён домен past-simple.ru** (reg.ru + Netlify). Apex запись `A @ → 75.2.60.5`, www записан туда же. HTTPS Let's Encrypt автоматический. `www.past-simple.ru` редиректит на apex.
- **Проект переименован: Chronos → Past Simple.** Затронуто: HTML titles, бренд в навигации, package.json (`past-simple`), глобалка `__pastSimple`, User-Agent, comment-headers, все md-документы, memory-файлы (содержимое; имена `chronos_*.md` остались). НЕ переименовано: GitHub-репо `Qrootto/chronos`, локальная папка `Chronos/`, Netlify site name (`coruscating-entremet-9241f5`), имена memory-файлов.

### Состояние кода — основное
- **Главный экран `index.html` + `js/main.js`** работает: люди с линиями жизни, точками событий, метки веков, year scale, sidebar.
- **Попап** работает: открывается по клику на точку события, рендерит фото, имя, годы жизни, lifetime-track с маркером, lead, секции (заголовок + body) для каждого подзаголовка.
- **Библиотека компонентов** в `/components.html` и токены в `/tokens.html` — обновляются при правках в `styles/components/*.css` и `styles/tokens.css`.
- Деплой: push в `main` → Netlify собирает → live на past-simple.ru за ~30-60 сек.

### Открытые долги (см. BACKLOG.md)
- **Факт-чек текстов** — приоритет №1 перед production. Все 181 секции — черновики, нужна сверка с Wikipedia / Britannica.
- Особый попап для связанных событий (пары двух людей с одной точкой).
- Расхождение timeline-mini в Header (Figma 32×405, у нас 36×445).
- Категория `musician` (токен + variant + проверка roster).
- Mobile/empty/loading состояния.
- Аналитика (Я.Метрика).

---

## С чего начать в следующей сессии

1. **Прочитать `MAIN_SCREEN.md`** — освежить ментальную модель.
2. Спросить пользователя про задачу. Скорее всего:
   - Полировка/баги — `npm run dev` в папке `Chronos/`, открыть `http://localhost:5174/past-simple.ru` для prod-проверки.
   - Факт-чек текстов — следующий пакет точечных правок (вариант C из BACKLOG: «Факт-чек текстов»).
   - Что-то из BACKLOG (попап-пара, мобайл, метрика).

---

## Очередь задач 2026-05-04 (закрыта)

Все пункты ниже **сделаны** ✅. Оставлен список как контекст того, что
обсудили и согласовали. Если детали поведения нужно уточнить — смотри
комментарии в коде (везде ссылки на MAIN_SCREEN.md и описания в JS/CSS).

### Структурные

1. **Library на `past-simple.ru/library`** (вариант **а**) — Netlify/CF redirect
   `/library` → `/components.html`. Можно добавить и `/library/tokens`,
   `/library/components` если понадобится позже.
2. **Раскрывающиеся секции под каждым компонентом** в `components.html` —
   ✅ Базовая версия: одна раскрывашка на весь компонент, после описания
   (см. `js/components-page.js`, `js/build/generate-components-meta.js`).
   ⚠️ **Доделать** — пользователь хочет отдельную раскрывашку под каждым
   **вариантом** (`.ks-state`) компонента, расположенную ниже самого
   варианта и **выше** раскрывашки HTML (`.ks-snippet`). Парсер должен
   собирать токены на уровне конкретного модификатора (`life-line--hovered`
   = базовые токены `.life-line` + специфичные для модификатора). UI:
   N раскрывашек на компонент вместо одной.

### Фичи (по порядку, согласованы)

3. **Year hover + Current year vertical line + возрасты в пересечениях**:
   - 3.1 Year при hover — размер шрифта как у Active.
   - 3.2 Новый компонент **Current year**: вертикальная линия 1px
     `--light-4` от центра кружка year до верхней границы Timeflow.
     Появляется при hover курсором над любой точкой в Timeflow / year-
     scale (соответствует году под курсором).
   - 3.3 В пересечениях линии Current year с life-line — справа от линии
     на 4px, выше life-line на 4px — текст «X лет/год/года» цветом
     `--light-16`. **Русское склонение** (1 год / 2 года / 5 лет).
     Не показываем если: year < born, year > died, или линия life-line
     обрезана краем экрана в этой точке.
4. **Life-line hover через event-dot** (НЕ при hover на саму life-line):
   - При hover на любой `event-dot` срабатывает hover-state у его
     life-line.
   - Цвет hovered life-line = категория человека
     (`--surface-person-<category>`).
5. **Default state на загрузке**:
   - 5.1 Центр экрана = разделитель между XIX и XX веком (= 1900 год).
     Это компонент event в состоянии Дивайдер века
     (`--surface-event-century`).
   - 5.2 На Timeflow всегда **10 рандомных людей**. Если пользователь
     заходил раньше — восстанавливаем его выбор из `localStorage`
     (ключ типа `past-simple:selected-people`).
6. **Sticky-имена** при скролле — отступ **4px** от ближайшего края
   экрана.
7. **Default horizontal scroll + hint** (частично уже сделано):
   - 7.1 Колесо мыши без shift → horizontal scroll. **Уже сделано**
     (см. js/main.js → setupWheel).
   - 7.2 С shift → vertical scroll по высоте контента. **Уже сделано**.
   - 7.3 Появляется хинт «shift + scroll» **только когда есть
     vertical scroll** (контент людей выше viewport). Позиция: левый
     нижний угол, выше year-scale на 4px и от левого края экрана 4px.
     Текст цветом нового токена `--text-secondary` = `--light-64`.
     Слева от текста — **анимированная иконка**: маленький скруглённый
     прямоугольник, плавно движется вверх-вниз с амплитудой 6px,
     цвет `--surface-icon-secondary`.

### Bug fix (уже сделано в сессии 2026-05-04)

- ✅ Zoom накладывался на year-scale на Windows — структурный фикс:
  scrollbar опущен в самый низ под year-scale, year-scale в bottom-bar,
  zoom над bottom-bar через `bottom: calc(100% + 8px)`.

### Параллельная задача — миграция с Netlify

Free tier Netlify теперь работает в credits: 1 deploy = 15 credits,
300 credits/мес = ~20 деплоев/мес. За день переименования + правок
исчерпали 315/300, проект paused до 1 июня.

Решение: миграция на **Cloudflare Pages** (бесплатно, 500 builds/мес,
unlimited bandwidth). DNS переезжает на Cloudflare nameservers
(заменяют reg.ru). HTTPS auto. После миграции — продолжаем по
очереди задач выше.

---

## Важные нюансы общения с пользователем

- Пользователь — дизайнер, не программист. В коде объяснять коротко, в тексте — простым языком (формулы через примеры).
- Не копировать макеты пиксель-в-пиксель. Реализуем правила, не статику. Hover-состояния в Figma — иллюстрация, не дефолт.
- Figma MCP экономим. Каждый фрейм читаем один раз → в `design-snapshot.md`. При rate-limit — пользователь экспортирует SVG/PNG в `Chronos/assets/`. Альтернативный fileKey для копии: `nDUthmmm6tov5YEgmgq2oL` (Sign-up).
- Большие SVG (>50k токенов): Read с `limit` или python/grep для извлечения координат, не читать целиком.
- Stop-хук в `.claude/settings.json` напомнит обновить SESSION.md (срабатывает если файл старше 10 мин).

---

## Файлы для быстрого доступа

| Что | Где |
|---|---|
| Ментальная модель экрана | `MAIN_SCREEN.md` |
| Дизайн-долг и беклог | `BACKLOG.md` |
| Снимок Figma (размеры, цвета) | `design-snapshot.md` |
| Схема данных | `data/_schema.js` |
| Геометрия попапа | `~/.claude/.../memory/chronos_popup_geometry.md` |
| Стартовая точка кода | `index.html` → `js/main.js` |
| Рендер Timeflow | `js/timeflow.js` |
| Рендер Year scale | `js/year-scale.js` |
| Раскладка main screen | `styles/main-screen.css` |
